<Lead>
  Layer probing answers a specific question: for some property you care
  about — speaker gender, accent, emotion, anything you can label — which
  layer of the model actually represents it best?
</Lead>

<Badges items={["layer_probe", "logreg", "linear_svm", "cross-validation"]} />

## What is layer probing?

A probe is a small, simple classifier trained on a model's hidden activations to predict a property, such as speaker gender or emotion. If a linear probe can predict the property from a layer, that layer represents it. Repeating this at every layer shows where in the network the property emerges.

## How it works

The `layer_probe` operation trains a small linear classifier — a
**probe** — on the hidden states from each layer independently, then
scores how well each layer's probe predicts the property. The result is a
per-layer view of where in the model that property becomes linearly
separable.

<img
  src="/assets/layer_probes.jpeg"
  alt="Diagram: an audio clip passes through a model's layers, Layer 1 to Layer 5. The same kind of probe is trained on each layer separately, and each probe's accuracy is drawn as a horizontal bar. Two dashed reference lines mark the shuffled-label control and the majority baseline. The bar for Layer 4 is highlighted as the best layer, reaching past both lines. Below: accuracy is how well the probe predicts, baseline is a guess by class size, and selectivity is accuracy minus the shuffled-label control."
  className="my-6 w-full rounded-2xl"
/>

## Probe types

- **`logreg`** — logistic regression.
- **`linear_svm`** — linear support vector machine.

## Limits

<CardGrid min={200}>
  <Card title="Properties" kicker="up to 8" meta="Per probing run" />
  <Card title="CV folds" kicker="2–10" meta="Cross-validation folds per probe" />
</CardGrid>

Each property is probed independently across all layers, using
cross-validation so the reported score isn't just an artifact of the
particular train/test split.

| Setting | Default | Meaning |
|---|---|---|
| `probe` | `logreg` | `logreg` or `linear_svm` |
| `cv_folds` | 5 | Cross-validation folds (2–10) |
| `project_dims` | 256 | Random projection applied to each layer's vector before probing (0–1024; `0` disables) |
| `min_class_count` | 5 | Minimum files a class needs to be included (2–50) |
| `include_control` | on | Also train a control probe on shuffled labels |

Activations are mean-pooled per layer and standardised before fitting. Files
labelled empty, `unknown`, `none`, or `n/a` are dropped as unannotated, and
a property needs at least **8 usable files** to be probed at all.

## The three numbers per layer

Read all three together — accuracy on its own can mislead:

- **`accuracy`** — mean cross-validated accuracy of the probe.
- **`majority_baseline`** — the share of the largest class. Accuracy at or
  below this means the layer carries **no** usable information about the
  property, however high the number looks.
- **`selectivity`** — `accuracy − control_accuracy`, where the control is the
  identical probe trained on shuffled labels. A probe that beats the
  baseline but not its control is memorising training rows rather than
  reading the representation.

Each layer also reports macro-F1 and the accuracy's standard deviation across
folds, and each property reports its best layer by accuracy and by
selectivity.

## Reading the results

- **Layer profile chart** — probe accuracy (or the relevant score) plotted
  against layer index, for each property. The layer where the curve peaks
  is where that property is most linearly represented.
- **Selectivity heatmap** — properties against layers, so you can compare
  multiple properties' profiles at once and see whether they peak at the
  same layer or different ones.
- **Confusion matrix** — per-property, per-layer, showing which classes
  the probe actually confuses at that layer.

<Callout title="Image placeholder: layer probe profile">
  Add `/assets/docs/probes-profile.png` — the layer probe panel showing the
  layer profile chart and selectivity heatmap for a multi-property run.
  Replace this callout with
  `![Layer probe profile](/assets/docs/probes-profile.png)` once the real
  image is uploaded.
</Callout>
